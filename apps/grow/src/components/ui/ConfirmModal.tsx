// Re-export the shared ConfirmationDialog component
// Note: The shared component doesn't support isLoading, so that feature is removed
export { ConfirmationDialog as ConfirmModal } from '@ainexsuite/ui';
